[Unit]
Description=Etcd Server
After=network.target

[Service]
Type=simple
WorkingDirectory={{ etcd.data_dir }}
EnvironmentFile=-{{ etcd.conf_dir }}/etcd.conf
User=etcd
ExecStart=/usr/bin/etcd
Restart=on-failure
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
